# blog-infrastructure
A centralized repo containing your API Gateway configuration (like NGINX), and a global docker-compose.yml file to spin up everything (databases, gateway, services) locally with one command.
